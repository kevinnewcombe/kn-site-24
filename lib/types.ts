export type ProjectType = {
  name: string,
  url: string,
  date: string,
  role: string,
  description: string,
  screenshot: {
    url: string,
    width: number,
    height: number
    description: string,
  }
}
